#include "tree.h"

int main(void) {
  histogram->c = 'a'; histogram->freq = 9;
  histogram->c = 'b'; histogram->freq = 8;
  histogram->c = 'c'; histogram->freq = 5;
  histogram->c = 'd'; histogram->freq = 4;
  histogram->c = 'e'; histogram->freq = 2;


  return 0;
}
